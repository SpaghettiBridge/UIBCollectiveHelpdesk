mode: structured
valueByMode:
  structured:
    fk_updated_by:
      value: '{{state.AccountData.account_id}}'
    fk_ticket_id:
      value: '{{ui.tbl_mainTickets3.selectedRow.data.id}}'
    change:
      value: >-
        {{state.AccountData.name}} updated a message text from:
        "{{state.CurrentMessage.message}}" to "{{ui.inp_message_text2.value}}".
  custom: ''
