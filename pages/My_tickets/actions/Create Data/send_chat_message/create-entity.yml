mode: structured
valueByMode:
  structured:
    fk_account_id:
      value: '{{state.AccountData.account_id}}'
    message:
      value: '{{steps.params.data.message}}'
    message_type:
      value: '{{steps.params.data.type}}'
    fk_ticket_id:
      value: '{{ui.tbl_mainTickets3.selectedRow.data.id}}'
    message_attachment:
      value: '{{steps.get_attachment_name.data}}'
  custom: ''
