mode: structured
valueByMode:
  structured:
    fk_account_id:
      value: '{{state.AccountData.account_id}}'
    agent_number:
      value: '{{state.randomNum}}'
    transcript:
      value: '{{JSON.stringify(ui.cht_brainlessAgent.messages)}}'
  custom: ''
