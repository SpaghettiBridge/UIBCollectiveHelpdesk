mode: structured
valueByMode:
  structured:
    fk_ticket_id:
      value: '{{ui.tbl_mainTickets.selectedRow.data.id}}'
    change:
      value: '{{state.AccountData.name}} has Re-Opened this ticket.'
    fk_updated_by:
      value: '{{state.AccountData.account_id}}'
  custom: ''
